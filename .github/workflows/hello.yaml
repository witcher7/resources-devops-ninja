name: Custom Environmnet Variables
on: push
env: 
 SUBSCRIBE: "SUPERMAN"
jobs: 
   hello:
    runs-on: ubuntu-latest
    env: 
      MEMORY: TOKYO
    steps:
      - name: memory check
        env: 
          NAME: "HINAGURA"
        run: | 
            echo "$MEMORY"
            echo "$NAME"
      - name: Name Check
        run: echo "$NAME"
        
