name: Custom Environmnet Variables
on: 
  push:
env: 
 SUBSCRIBE: "SUPERMAN"
 NAME: "HINAGURA"
jobs: 
   hello:
    runs-on: ubuntu-latest
    env: 
      MEMORY: TOKYO
    steps:
      - name: memory check
        run: | 
            echo "$MEMORY"
            echo "$NAME"
      - name: Name Check
        run: |
         echo "$NAME"
         echo "$MEMORY"
         echo "${{ secrets.MYNAME }}
